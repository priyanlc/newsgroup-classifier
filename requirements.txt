# Shared dependencies for prepare_data.py (CPU-only, no GPU needed).
# Platform-specific dependencies are in cuda/requirements.txt and mlx/requirements.txt.
transformers>=4.36.0
scikit-learn>=1.3.1
numpy>=1.24.0
